# Dependencies
node_modules/

# Logs / editor cruft
*.log
.DS_Store
.vscode/
.idea/

# TS build info
*.tsbuildinfo

# NOTE: the served artifact is committed on purpose —
#   index.html, *.css, and the compiled .build/*.js ARE the GitSite deploy
#   artifact. Do NOT ignore them. The *.ts sources are committed too so the
#   artifact is rebuildable with `npm run build`.
