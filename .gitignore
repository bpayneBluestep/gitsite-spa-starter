# Dependencies
node_modules/

# Logs / editor cruft
*.log
.DS_Store
.vscode/
.idea/

# TS build info
*.tsbuildinfo

# NOTE: the served artifact is committed on purpose —
#   index.html + assets/app.<hash>.{js,css} ARE the GitSite deploy artifact,
#   built from src/ by `npm run build`. Do NOT ignore them.
