{
  "name": "configec-crm-gitsite",
  "private": true,
  "version": "3.0.0",
  "description": "ConfigEC Consultant CRM — global-script SPA, type-checked and bundled, served as a BlueStep GitSite under /spa/.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.9.3"
  }
}
