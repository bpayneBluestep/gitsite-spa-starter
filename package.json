{
  "name": "configec-crm-gitsite",
  "private": true,
  "version": "2.0.0",
  "description": "ConfigEC Consultant CRM — a global-script SPA served as a BlueStep GitSite under /spa/.",
  "scripts": {
    "clean": "node scripts/clean-artifact.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
